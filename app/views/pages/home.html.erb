<h1>Les travaux de Caterina</h1>

<ul>
  <% @items.each do |item| %>
    <li><%= item.title %></li>
    <li><%= item.description %></li>
    <li><%= item.price %></li>
    <li><%=cl_image_tag item.photo.key, height: 300, width: 400, crop: :fill %></li>
  <% end %>
</ul>